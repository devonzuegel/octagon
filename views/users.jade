extends layout
block topRight  

block mainContent
  .row.subheader
    .col-xs-12
      h3: a(href='#').selected Overview
      h3: a(href='#') Financials
      h3: a(href='#') Board Notes
      h3: a(href='#') Communication
  
  .row
    .col-xs-8
      .card#about
        #edit
        h2 About

        if (p && p.description)
            p #{p.description}
        else
            h5: a
              span.glyphicon.glyphicon-plus.loginout
              .loginout Add description
        

        if (p && p.homepage_url) 
            a(href=homepage_url).loginout
              <strong> #{p.homepage_url} </strong>
        else
            h5: a
              span.glyphicon.glyphicon-plus.loginout
              .loginout Add homepage url

      .card#milestones
        #edit
        h2 Milestones
        p Milestone 1
        p Milestone 2

      .card#team
        #edit
        h2 Team
        p Joe Schmo
        p Bilbo Baggins

      .card#test
        #edit
        h2 Test
        p "Sed ut perspiciatis unde omnis iste natus error sit voluptatem accusantium doloremque laudantium, totam rem aperiam, eaque ipsa quae ab illo inventore veritatis et quasi architecto beatae vitae dicta sunt explicabo. Nemo enim ipsam voluptatem quia voluptas sit aspernatur aut odit aut fugit, sed quia consequuntur magni dolores eos qui ratione voluptatem sequi  in ea voluptate velit esse quam nihil molestiae consequatur, vel illum qui dolorem eum fugiat quo voluptas nulla pariatur?"

    .col-xs-4
      .card.text-center#img
        .pull-right(style='margin-right:13px'): #edit(style='position:absolute')
        if (p && p.img_path)
          img(src=p.img_path)
          p.quote #{p.short_descrip}
        else
          .text-left: h5: a
            span.glyphicon.glyphicon-plus.loginout
            .loginout add logo

        
      
      .blank: p
        h4 <strong>Founded:</strong> 
        if (p && p.founded_on)
          | #{p.founded_on}
        else
          h5: a  add date
        br


        h4 <strong>Founders:</strong> 
        if (p && p.founders)
          each f,i in p.founders
            if (i !=0)
              | , 
            a(href='http://api.crunchbase.com/v/2/' + f.path + '?user_key=c7653c536c2266d0da1155557600c8a4') #{f.name}
            //- TODO hide our userkey
        else
          h5: a add founders
        br


        h4 <strong>Funding:</strong> 
        if (p && p.total_funding)
          | $#{p.total_funding.toFixed(2).replace(/\d(?=(\d{3})+\.)/g, '$&,')}
        else
          h5: a add funding info  
        br


        h4 <strong>Categories:</strong> 
        if (p && p.categories)
          each c,i in p.categories
            if (i !=0)
              | , 
            a #{c.name}
        else
          h5: a add tags


  script.
    var cards = ['#about', '#milestones', '#team', '#test', '#img'];

    for (var i = 0; i < cards.length; i++) {
      $(cards[i]).hover(function() {
                          var edit = '<div class="h5 pull-right"><a><span class="glyphicon glyphicon-edit"></div></a></div>'
                          $(this).find('#edit').append(edit)
                        }, function(){
                          $(this).find('#edit').children("div").remove();
                        }
      );
    }
