extends layout
block topRight  
  //- .tooltip_a(style='margin-top: 15px')
    a(href='../new_photo/'  data-toggle="tooltip" data-original-title="Update logo")
      span.glyphicon.glyphicon-edit

block mainContent
  .row.subheader
    .col-xs-12
      h3: a(href='#').selected Overview
      h3: a(href='#') Financials
      h3: a(href='#') Board Notes
      h3: a(href='#') Communication
      //- h3.sub: a(href='#') Edit
  
  .row
    .col-xs-8
      .card
        h2 About

        if (profile && profile.description)
            p #{profile.description}
        else
            h5: a
              span.glyphicon.glyphicon-plus.loginout
              .loginout Add description
        

        if (profile && profile.homepage_url) 
            a(href=homepage_url).loginout
              <strong> #{profile.homepage_url} </strong>
        else
            h5: a
              span.glyphicon.glyphicon-plus.loginout
              .loginout Add homepage url


      .card
        h2 Milestones
        p Milestone 1
        p Milestone 2

      .card
        h2 Team
        p Joe Schmo
        p Bilbo Baggins

      .card: p "Sed ut perspiciatis unde omnis iste natus error sit voluptatem accusantium doloremque laudantium, totam rem aperiam, eaque ipsa quae ab illo inventore veritatis et quasi architecto beatae vitae dicta sunt explicabo. Nemo enim ipsam voluptatem quia voluptas sit aspernatur aut odit aut fugit, sed quia consequuntur magni dolores eos qui ratione voluptatem sequi  in ea voluptate velit esse quam nihil molestiae consequatur, vel illum qui dolorem eum fugiat quo voluptas nulla pariatur?"

    .col-xs-4
      .card.text-center

        if (profile && profile.img_path)
          img(src=profile.img_path)
          p.quote #{profile.short_descrip}
        else
          .text-left: h5: a
            span.glyphicon.glyphicon-plus.loginout
            .loginout add logo

        
        
      .blank: p
        h4 Founded: 
        if (profile && profile.founded_on)
          | #{profile.founded_on}
        else
          h5: a  add date
        br

        h4 Founders: 
        if (profile && profile.founders)
          | #{profile.founders}
          //- each f in profile.founders
          //- | #{f.name} 
        else
          h5: a add founders
        br

        h4 Funding: 
        if (profile && profile.total_funding)
          | $#{profile.total_funding.toFixed(2).replace(/\d(?=(\d{3})+\.)/g, '$&,')}
        else
          h5: a add funding info  
        br

        h4 Categories: 
        if (profile && profile.founders)
          | #{profile.founders}
        else
          h5: a add tags